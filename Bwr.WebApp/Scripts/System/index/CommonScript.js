
var CountryTable = $('#CountryTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});


var GroupTable = $('#GroupTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var UserTable = $('#UserTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var ProvinceTable = $('#ProvinceTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var branchTable = $('#branchTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var RevenueTable = $('#RevenueTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var ExpensesTable = $('#ExpensesTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var ClientTable = $('#ClientTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var CoinTable = $('#CoinsTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var CompanyTable = $('#CompanyTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var AttatchmentTable = $('#AttatchmentTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var TreasuryTable = $('#TreasuryTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var BalnceTable = $('#BalnceTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var PrivilegesTable = $('#PrivilegesTable').DataTable({
    "ordering": true,
    "columnDefs": [
        { "targets": [0], "visible": false, "searchable": false },
        {
            "orderable": false, targets: "no-sort"

        }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});

var comissionTalbe = $('#comissionTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});
var exchangeTable = $('#exchangeTable').DataTable({
    "ordering": true,
    "columnDefs": [{
        "orderable": false, targets: "no-sort"
    }],
    "language": {
        "search": "البحث",
        "info": "عرض _START_ إلى _END_ من _كل_ العناصر",
        "emptyTable": "لا يوجد اي عناصر للعرض",
        "infoEmpty": "لا يوجد اي عناصر",
        "zeroRecords": "لا يوجد اي عناصر مطابقة",
        "infoFiltered": "",
    }
});


function FuncPassword(text) {
    if (text == undefined || text == "" || text == "NULL" || text == "null")
        return "";
    else
        return "*****";
}
//for button and div 
//1 - class , 2 - name, 3 - visbleName 4 - onclick 5 - width
//for txt
//  1-type 2- class 3- vlaue
//for toggle
//2- class 3- lable 4- herf
function _CreateElementAndSetAttribute(element, ArrayOfAtt, ToBeappended) {
    if (ArrayOfAtt != undefined) {
        for (var t = 0; t < ArrayOfAtt.length; t++) {
            if (element == "div") {
                if (ToBeappended == undefined) {
                    var div = document.createElement("div");
                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        div.id = ArrayOfAtt[0];
                    }
                    if (ArrayOfAtt.length == 2) {
                        div.id = ArrayOfAtt[0];
                        div.setAttribute("class", ArrayOfAtt[1]);
                    }
                    return div;
                }
                else {
                    var div = document.createElement("div");
                    ToBeappended.appendChild(div);
                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        div.id = ArrayOfAtt[0];
                    }
                    if (ArrayOfAtt.length == 2) {
                        div.id = ArrayOfAtt[0];
                        div.setAttribute("class", ArrayOfAtt[1]);
                    }
                    return div;

                }
            }
            if (element == "btn") {
                if (ToBeappended == undefined) {
                    var btn = document.createElement("button");
                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                    }
                    if (ArrayOfAtt.length == 2) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                    }
                    if (ArrayOfAtt.length == 3) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];
                    }
                    if (ArrayOfAtt.length == 4) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];
                        btn.setAttribute("onclick", ArrayOfAtt[3]);
                    }
                    if (ArrayOfAtt.length == 5) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];
                        btn.setAttribute("onclick", ArrayOfAtt[3]);
                        btn.style.width = ArrayOfAtt[4];
                    }
                    return btn;

                }
                else {
                    var btn = document.createElement("button");
                    ToBeappended.appendChild(btn);

                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                    }
                    if (ArrayOfAtt.length == 2) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                    }
                    if (ArrayOfAtt.length == 3) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];

                    }
                    if (ArrayOfAtt.length == 4) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];
                        btn.setAttribute("onclick", ArrayOfAtt[3]);
                    }
                    if (ArrayOfAtt.length == 5) {
                        btn.setAttribute("class", ArrayOfAtt[0]);
                        btn.setAttribute("name", ArrayOfAtt[1]);
                        btn.innerText = ArrayOfAtt[2];
                        btn.setAttribute("onclick", ArrayOfAtt[3]);
                        btn.style.width = ArrayOfAtt[4];
                    }
                    return btn;
                }
            }
            if (element == "txt") {
                if (ToBeappended == undefined) {
                    var text = document.createElement("input");
                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                    }
                    if (ArrayOfAtt.length == 2) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                        text.setAttribute("class", ArrayOfAtt[1]);
                    }
                    if (ArrayOfAtt.length == 3) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                        text.setAttribute("class", ArrayOfAtt[1]);
                        text.value = ArrayOfAtt[2];
                    }
                    return text;
                }
                else {
                    var text = document.createElement("input");
                    ToBeappended.appendChild(text);
                    if (ArrayOfAtt.length == 0) {
                    }
                    if (ArrayOfAtt.length == 1) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                    }
                    if (ArrayOfAtt.length == 2) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                        text.setAttribute("class", ArrayOfAtt[1]);
                    }
                    if (ArrayOfAtt.length == 3) {
                        text.setAttribute("type", ArrayOfAtt[0]);
                        text.setAttribute("class", ArrayOfAtt[1]);
                        text.value = ArrayOfAtt[2];
                    }
                    return text;
                }
            }

            if (element == "toggle") {
                // onclick= "PrivilegesbtnEdit();" 
                var div = '<div class={' + ArrayOfAtt[0] + '><div class="form-group" ><div class="caption font-red-sunglo"> <a  data-toggle="modal" href=' + ArrayOfAtt[2] + '> <button type="button" class="btn btn-default" id = ' + ArrayOfAtt[3] + '> ' + ArrayOfAtt[1] + '</button> </a > </div > </div >  </div > ';
                return div;
            }
        }
    }
    else {
        if (element == "div") {
            var div = document.createElement("div");
            return div;
        }
        if (element == "btn") {
            var btn = document.createElement("button");
            return btn;
        }
    }
}
function _creatDivFunction(id, TableName) {
    var ChoicDiv = _CreateElementAndSetAttribute("div", [TableName + "choicDiv"]);
    var ViewDiv = _CreateElementAndSetAttribute("div", [TableName + "viewDiv", "col-lg-4"], ChoicDiv);
    var EditDiv = _CreateElementAndSetAttribute("div", [TableName + "EditDiv", "col-lg-4"], ChoicDiv);
    var DltDiv = _CreateElementAndSetAttribute("div", [TableName + "DltDiv", "col-lg-4"], ChoicDiv);

    var AttViewBtn = ["btn btn-primary", TableName + "ViewBtn", "عرض"];
    var AttEditBtn = ["btn btn-secondary", TableName + "EditBtn", "تعديل"];
    var AttDeleteBtn = ["btn btn-danger", TableName + "DeleteBtn", "حذف", TableName + "DeleteFuncrtion(this," + id + ");"];
    btnView = _CreateElementAndSetAttribute("btn", AttViewBtn, ViewDiv);
    btnView.id = "btnView";

    EditView = _CreateElementAndSetAttribute("btn", AttEditBtn, EditDiv);
    EditView.id = "EditView";
    DltView = _CreateElementAndSetAttribute("btn", AttDeleteBtn, DltDiv);
    DltView.id = "DltView";


    var UpdateDiv = _CreateElementAndSetAttribute("div", [TableName + "UpdateDiv"]);
    UpdateDiv.style.display = "none";
    var SaveDiv = _CreateElementAndSetAttribute("div", [TableName + "SaveDiv", "col-lg-6"], UpdateDiv);
    var cancelDiv = _CreateElementAndSetAttribute("div", [TableName + "cancelDiv", "col-lg-6"], UpdateDiv);
    AttSaveBtn = ["btn btn-success", TableName + "SaveBtn", "حفظ", TableName + "SaveFuncrtion(this," + id + ");", "100px"];
    SaveBtn = _CreateElementAndSetAttribute("btn", AttSaveBtn, SaveDiv);
    AttCancelBtn = ["btn btn-danger", TableName + "CancelBtn", "إلغاء", TableName + "CancelFuncrtion(this," + id + ");", "100px"];
    cancelBtn = _CreateElementAndSetAttribute("btn", AttCancelBtn, cancelDiv);
    return ChoicDiv.outerHTML + UpdateDiv.outerHTML;
}
$(document).ready(function () {
    if (HavePrivlege(priveleges.showCountry)) {
        FillCountryTable();
        FillProvinceTable();
        FillprovinceCountreySelect();
    }
    FillCoinTable();
    fillCompanyTable();
    
    FillRevenueTable();
    FillExpensesTable();
    fillClientTable();
    FillBlanceTable();
    FillAttachmentTable();
    FillTreasuryTable();
    if (HavePrivlege(priveleges.showGroup)) {
        FillTableWithGroup();
    }
    //FillTableWithPrivileges();
    if (HavePrivlege(priveleges.addGroup)) {
        FillPrivilegesSelect();
    }

    fillExhchnage();
    FillUserTable();
});
function FlipYesOrNoToBoolean(answer) {
    if (answer === "نعم")
        return true;
    return false;

}

function FlipBoolToYesOrNo(status) {
    if (status)
        return "نعم";
    return "لا";
}

class _div {
    constructor() { };
    static _getdive(element, NumOfParent, arraySpiling, ArrayVsvisible) {
        if (NumOfParent != undefined) {
            if (NumOfParent == 2)
                return element.parentElement.parentElement;
            else if (NumOfParent == 1)
                return element.parentElement;
            else if (NumOfParent == 3)
                return element.parentElement.parentElement.parentElement;
            else if (NumOfParent == 4)
                return element.parentElement.parentElement.parentElement.parentElement;
        }
        if (arraySpiling != undefined) {
            if (bool = true)
                div.nextSibling;
            else
                ;

        }
        if (isvisible) {
            if (bool == false)
                div.style.display = "none";
            else
                div.style.display = "block";

        }
    }

    static _creatDiv(PrimaryDiv, iscreated, ArrayOfIncludingDivs, JsonOfAttribute) {
        if (iscreated) {
            if (JsonOfAttribute != undefined) {
                for (var Attribute in JsonOfAttribute) {
                    PrimaryDiv.setAttribute(Attribute, JsonOfAttribute[Attribute]);
                }
            }
            if (ArrayOfIncludingDivs != undefined) {
                for (var t = 0; t < ArrayOfIncludingDivs.length; t++) {
                    PrimaryDiv.appendChild(ArrayOfIncludingDivs[t]);
                }
            }
        }
        else {
            var PrimaryDiv = document.createElement("div");
            if (JsonOfAttribute != undefined) {
                for (var Attribute in JsonOfAttribute) {
                    PrimaryDiv.setAttribute(Attribute, JsonOfAttribute[Attribute]);
                }
            }
            if (ArrayOfIncludingDivs != undefined) {
                for (var t = 0; t < ArrayOfIncludingDivs.length; t++) {
                    PrimaryDiv.appendChild(ArrayOfIncludingDivs[t]);
                }
            }
        }
        return PrimaryDiv;
    }
    static _creatDivFunction(id, TableName, type) {
        if (type == undefined) {
            var choiceDIv = document.createElement("div");
            var viewDiv = document.createElement("div");
            viewDiv.setAttribute("class", "col-lg-4");
            var editDiv = viewDiv.cloneNode();
            var DeleteDiv = viewDiv.cloneNode();

            choiceDIv.appendChild(viewDiv);
            choiceDIv.appendChild(editDiv);
            choiceDIv.appendChild(DeleteDiv);

            var viewButton = document.createElement("button");
            viewDiv.appendChild(viewButton);
            viewButton.setAttribute("class", "btn btn-primary");
            viewButton.setAttribute("name", TableName + "ViewBtn");
            viewButton.setAttribute("onclick", TableName + "ViewFuncrtion(" + id + ");");
            viewButton.innerText = "عرض";

            var editBtn = document.createElement("button");
            editDiv.appendChild(editBtn);
            editBtn.setAttribute("class", "btn btn-secondary");
            editBtn.setAttribute("name", TableName + "EditBtn");
            editBtn.setAttribute("onclick", TableName + "EditFuncrtion(this);");
            editBtn.innerText = "تعديل";

            var deleteBtn = document.createElement("button");
            DeleteDiv.appendChild(deleteBtn);
            deleteBtn.setAttribute("class", "btn btn-danger");
            deleteBtn.setAttribute("name", TableName + "DeleteBtn");
            deleteBtn.setAttribute("onclick", TableName + "DeleteFuncrtion(this," + id + ");");
            deleteBtn.innerText = "حذف";
            var updateDiv = document.createElement('div');
            updateDiv.style.display = "none";

            var SaveDiv = document.createElement("div");
            SaveDiv.setAttribute("class", "col-lg-6");
            var CancelDiv = SaveDiv.cloneNode();

            updateDiv.appendChild(SaveDiv);
            updateDiv.appendChild(CancelDiv);

            var savebtn = document.createElement("button");
            SaveDiv.appendChild(savebtn);
            savebtn.setAttribute("class", "btn btn-success");
            savebtn.setAttribute("onclick", TableName + "SaveFuncrtion(this," + id + ");");
            savebtn.style.width = "100px";
            savebtn.innerText = "حفظ";

            var cancelBtn = document.createElement('button');
            CancelDiv.appendChild(cancelBtn);
            cancelBtn.setAttribute("class", "btn btn-danger");
            cancelBtn.setAttribute('onclick', TableName + 'CancelFuncrtion(this,' + id + ');');
            cancelBtn.innerText = "إلغاء";
            cancelBtn.style.width = "100px";
            CancelDiv.appendChild(cancelBtn);
            return choiceDIv.outerHTML + updateDiv.outerHTML;
        }
        if (type == "choice") {
            var choiceDIv = document.createElement("div");
            var viewDiv = document.createElement("div");
            viewDiv.setAttribute("class", "col-lg-4");
            var editDiv = viewDiv.cloneNode();
            var DeleteDiv = viewDiv.cloneNode();

            choiceDIv.appendChild(viewDiv);
            choiceDIv.appendChild(editDiv);
            choiceDIv.appendChild(DeleteDiv);

            var viewButton = document.createElement("button");
            viewDiv.appendChild(viewButton);
            viewButton.setAttribute("class", "btn btn-primary");
            viewButton.setAttribute("name", TableName + "ViewBtn");
            viewButton.innerText = "عرض";

            var editBtn = document.createElement("button");
            editDiv.appendChild(editBtn);
            editBtn.setAttribute("class", "btn btn-secondary");
            editBtn.setAttribute("name", TableName + "EditBtn");
            editBtn.setAttribute("onclick", TableName + "EditFuncrtion(this);");
            editBtn.innerText = "تعديل";

            var deleteBtn = document.createElement("button");
            DeleteDiv.appendChild(deleteBtn);
            deleteBtn.setAttribute("class", "btn btn-danger");
            deleteBtn.setAttribute("name", TableName + "DeleteBtn");
            deleteBtn.setAttribute("onclick", TableName + "DeleteFuncrtion(this," + id + ");");
            deleteBtn.innerText = "حذف";
            return choiceDIv.outerHTML;

        }
        if (type == "Update") {
            var updateDiv = document.createElement('div');
            updateDiv.style.display = "none";

            var SaveDiv = document.createElement("div");
            SaveDiv.setAttribute("class", "col-lg-6");
            var CancelDiv = SaveDiv.cloneNode();

            updateDiv.appendChild(SaveDiv);
            updateDiv.appendChild(CancelDiv);

            var savebtn = document.createElement("button");
            SaveDiv.appendChild(savebtn);
            savebtn.setAttribute("class", "btn btn-success");
            savebtn.setAttribute("onclick", TableName + "SaveFuncrtion(this," + id + ");");
            savebtn.style.width = "100px";
            savebtn.innerText = "حفظ";

            var cancelBtn = document.createElement('button');
            CancelDiv.appendChild(cancelBtn);
            cancelBtn.setAttribute("class", "btn btn-danger");
            cancelBtn.setAttribute('onclick', TableName + 'CancelFuncrtion(this,' + id + ');');
            cancelBtn.innerText = "إلغاء";
            cancelBtn.style.width = "100px";
            CancelDiv.appendChild(cancelBtn);
            return updateDiv.outerHTML;
        }
    }

};


class _Element {
    constructor() { };
    static _creatElement(TypeOfElement, TyepOfAttribute_ActionController, JsonOfAttribute, Value) {
        if (TypeOfElement != undefined) {
            if (TypeOfElement == "input") {
                if (TyepOfAttribute_ActionController == "text") {
                    var textBox = document.createElement("input");
                    for (var Attribute in JsonOfAttribute) {
                        textBox.setAttribute(Attribute, JsonOfAttribute[Attribute]);
                    }
                    textBox.value = Value;
                    return textBox;
                }
                if (TyepOfAttribute_ActionController == "checkbox") {
                    var checkBox = document.createElement("input");
                    for (var Attribute in JsonOfAttribute) {
                        button.setAttribute(Attribute, JsonOfAttribute[Attribute]);
                    }
                    checkBox.checked = Value;
                    return checkBox;
                }
            }
            if (TypeOfElement == "button") {
                var button = document.createElement("button");
                if (JsonOfAttribute != undefined) {
                    for (var Attribute in JsonOfAttribute) {
                        if (Attribute == "width") {
                            button.style.width = JsonOfAttribute[Attribute];
                        }
                        else
                            button.setAttribute(Attribute, JsonOfAttribute[Attribute]);
                    }
                }
                button.innerText = Value;
                return button;
            }
            if (TypeOfElement == "select") {
                var DropDownList = document.createElement("select");
                $.post(
                    TyepOfAttribute_ActionController,
                    function (data) {
                        for (var i = 0; i < data.length; i++) {
                            var option = document.createElement("option");
                            option.value = data[i].Object.getOwnPropertyNames(JsonOfAttribute).sort()[0];
                            option.innerText = data[i].Object.getOwnPropertyNames(JsonOfAttribute).sort()[1];
                            if (data[i].id == value)
                                option.selected = true;
                            DropDownList.appendChild(option);
                        }
                    }
                )
                return DropDownList;
            }



        }
    };

    //static _creatElementselect(TypeOfElement, ActionController, arrayOfdata, value) {
    //    var DropDownList = document.createElement("select");
    //    $.post(
    //        ActionController,
    //        function (data) {
    //            for (var i = 0; i < data.length; i++) {
    //                var option = document.createElement("option");
    //                option.value = data[i].Object.getOwnPropertyNames(arrayOfdata).sort()[0];
    //                option.innerText = data[i].Object.getOwnPropertyNames(arrayOfdata).sort()[1];
    //                if (data[i].id == value)
    //                    option.selected = true;
    //                DropDownList.appendChild(option);
    //            }
    //        }

    //    )
    //};


};

class _Table {
    constructor() { };
    static _buildTableOfUpdate(location, NumOfdt, appended) {
        //getvalue
        //var row = rowC;
        var dt;
        for (var t = 0; t < tdC; t++) {
            dt[t] = rowC.parentElement.parentElement.getElementsByTagName("td");
            //if (FlipYesOrNoToBoolean(dt[t].innerText))
            //    ReturnValue[t] = FlipYesOrNoToBoolean(dt[t].innerText);
            //else
            //    ReturnValue[t] = dt[t].innerText;

        }
        //
        //for (var t = 0; t < tdC; t++) {

        dt[0].appendChild(text);
        dt[1].appendChild(check);

        //}

    }
}